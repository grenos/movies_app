import React, { memo } from 'react';
import { CustomText } from '@Components';

export const ListPlaceholder = memo(() => {
    return <CustomText font="body">"There is nothing here... yet"</CustomText>;
});
